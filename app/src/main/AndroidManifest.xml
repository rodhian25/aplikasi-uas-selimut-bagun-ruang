<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rodhian.appuasvolumeselimut">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mrodhiansyabri"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PilihanActivity" />
        <activity android:name=".HalamanKubusActivity" />
        <activity android:name=".HalamanKerucutActivity" />
        <activity android:name=".HalamanBalokActivity" />
        <activity android:name=".VolumeKubusActivity" />
        <activity android:name=".LuasKubusActivity" />
        <activity android:name=".LuasBalokActivity" />
        <activity android:name=".VolumeKerucutActivity" />
        <activity android:name=".LuasKerucutActivity" />
        <activity android:name=".VolumeBalokActivity"></activity>
    </application>

</manifest>